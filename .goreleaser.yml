# GoReleaser configuration
# https://goreleaser.com/customization/
# Docs: https://goreleaser.com
version: 2

project_name: vsp

before:
  hooks:
    - go mod tidy

builds:
  - id: vsp
    main: ./cmd/vsp
    binary: vsp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.Version={{.Version}}
      - -X main.Commit={{.Commit}}
      - -X main.BuildDate={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
      - "386"
      - arm
    goarm:
      - "7"
    ignore:
      # macOS only supports amd64 and arm64
      - goos: darwin
        goarch: "386"
      - goos: darwin
        goarch: arm
      # Windows arm (32-bit) is not a standard target
      - goos: windows
        goarch: arm

# Plain binaries — no zip/tar.gz, matches existing release convention
archives:
  - id: default
    formats: [binary]
    name_template: "{{ .ProjectName }}-{{ .Os }}-{{ .Arch }}"

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

# git-cliff generates RELEASE_NOTES.md separately in the workflow.
# GoReleaser v2.15+ dropped changelog.use: git-cliff, so we disable here.
changelog:
  disable: true

release:
  draft: false
  # Let GoReleaser infer the authenticated repository from the checkout.
  prerelease: auto
  name_template: "v{{ .Version }}"
  footer: |
    Community-maintained distribution. Review the release notes and checksums before installation.
